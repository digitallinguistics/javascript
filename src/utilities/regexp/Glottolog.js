/**
 * A regular expression for Glottolog Language Codes.
 * @memberof utilities.regexp
 * @instance
 * @type {RegExp}
 */
const glottoCodeRegExp = /^[a-z]{4}[0-9]{4}$/u;

export default glottoCodeRegExp;
