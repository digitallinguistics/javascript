const { models: { MultiLangString } } = require(`../../test`);

describe(`MultiLangString`, () => {

  it(`is a MultiLangString class`, () => {
    expect(MultiLangString.name).toBe(`MultiLangString`);
  });

});
